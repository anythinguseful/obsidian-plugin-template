# Arena adapter: research

Use `agents/skills/vendor/mattpocock/research/SKILL.md`.

## Overrides

1. Do **not** dispatch a background agent. Research in this session.
2. Primary sources only (raw repo, npm types, specs). Docs pages are hypotheses.
3. Write the note from `docs/studies/_TEMPLATE.md` under `docs/studies/`.
4. Relative Markdown links; required frontmatter. Then `npm run check:docs`.
