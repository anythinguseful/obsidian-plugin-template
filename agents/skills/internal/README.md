# Internal development skills

Project-owned. They override vendor skills on conflict.

- `plugin-ui` — binding UI/CSS/copy contract
- `functional-ui` — arrangement of settings and panels
- `plugin-docs` — where docs live and how they are formatted

Vendor skills never override these three. Grilling still uses one `ask_user` question per card.
