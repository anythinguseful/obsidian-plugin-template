# Arena TDD Adapter

Use the pinned `tdd` vendor skill. When it refers to `CONTEXT.md`, read
`AGENTS.md`, `docs/working-agreement.md`, and the relevant source/test instead.

For this plugin, write the smallest direct/unit regression first where possible,
then add a real-DOM or browser witness when the bug is visual, interactive, or
cross-surface. Preserve the project's existing smoke guards and run the gate
specified by `CONTRIBUTING.md` before completion.
